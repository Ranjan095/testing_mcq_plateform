const initialState = {
  isLoading: false,
  isSuccess: false,
  isError: false,
  fullName: "",
};

export const authReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
