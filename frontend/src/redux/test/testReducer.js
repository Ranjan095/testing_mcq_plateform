const initialState = {
  isLoading: false,
  isSuccess: false,
  isError: false,
  test: [],
};

export const testReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
